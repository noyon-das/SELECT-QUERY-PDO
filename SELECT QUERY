<?php 

	//$stmt = $pdo->query("SELECT * FROM my_tbl");
	//$user = $stmt->fetch();

	$stmt = $pdo->query("SELECT * FROM my_tbl");
	while ($row = $stmt->fetch()){
		
		echo "Name: " . $row['Name'] . " " ;
		echo "Class: " . $row['Class']."<br />";
		
	}
?>
